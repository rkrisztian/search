rootProject.name = 'search'

include 'search-app'

gradle.beforeProject { project ->
	project.with {
		buildscript {
			repositories {
				mavenCentral()
			}
		}

		repositories {
			mavenCentral()
		}

		version = BUILD_VERSION
		group = GROUP_ID
	}
}
